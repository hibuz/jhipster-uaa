apply plugin: "war"

bootWar {
    mainClassName = "com.hibuz.uaa.UaaApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
